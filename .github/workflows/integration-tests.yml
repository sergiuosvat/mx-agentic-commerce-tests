name: Integration Tests

on:
  push:
    branches: [main, master]
  pull_request:

concurrency:
  group: integration-${{ github.ref }}
  cancel-in-progress: true

jobs:
  integration:
    runs-on: ubuntu-latest
    timeout-minutes: 180
    env:
      COOLDOWN: 8
      MAX_PORT_WAIT: 45
      RETRY_DELAY: 8
      FAIL_ON_RETRY: 1

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Install Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'

      - name: Verify Node.js version
        run: node -v

      - name: Install sc-meta
        run: cargo install multiversx-sc-meta --locked

      - name: Setup ecosystem
        run: ./setup.sh

      - name: Install test repo npm dependencies
        run: npm ci

      - name: Run integration tests (one suite/package at a time)
        run: ./run_all_tests.sh --integration

      - name: Upload test logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: test-logs
          path: target/test-logs/
          retention-days: 7
